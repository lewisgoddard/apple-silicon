---
permalink: "api/search.json"
---
[{% for chip in collections.chipPagesCollection %}{"type":"chip","id":"{{ chip.id }}","name":"{{ chip.name }}","detail":"{% if chip.specs.cpu_cores and chip.specs.gpu_cores %}{{ chip.specs.cpu_cores }}-core CPU / {{ chip.specs.gpu_cores }}-core GPU{% elif chip.specs.cpu_cores %}{{ chip.specs.cpu_cores }}-core CPU{% else %}{{ chip.rank.rankDescription if chip.rank and chip.rank.rankDescription else chip.name }}{% endif %}","url":"{{ chip.url }}"}{% if not loop.last or collections.devicePagesCollection | length > 0 %},{% endif %}{% endfor %}{% for device in collections.devicePagesCollection %}{"type":"device","id":"{{ device.id }}","name":"{{ device.name }}","detail":"{{ device.categoryName }}{% if device.variants %} · {{ device.variants | length }} chip{{ 's' if device.variants | length != 1 }}{% endif %}","url":"/devices/{{ device.categoryId | lower }}/{{ device.id | lower }}/"}{% if not loop.last %},{% endif %}{% endfor %}]
