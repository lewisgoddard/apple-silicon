---
layout: base.njk
title: "Current Mac Desktop Lineup"
description: "Compare the chips available in every current Mac desktop — iMac, Mac mini, Mac Studio."
permalink: "current/macs/index.html"
---

{% from "macros/breadcrumbs.njk" import breadcrumbs %}
{% from "macros/compare-table.njk" import compareTable %}
{% from "macros/device-grid.njk" import deviceGrid %}

{{ breadcrumbs([
  { label: "Current Mac Desktop" }
]) }}

<h1>Current Mac Desktop Lineup</h1>
<p>
  The Mac mini leads the desktop lineup with the M6 — a 12-core CPU and dual 16-core Neural Engine in the smallest Mac — and steps up to the M5 Pro for professional creative work. The iMac remains on the M4, a capable all-in-one for everyday use. The Mac Studio's M5 Max and M5 Ultra top the range, with the M5 Ultra's 80-core GPU and 512 GB of unified memory the most of any current Mac.
</p>

{% set devices = getCurrentDevices("mac", "desktop") %}
{{ deviceGrid(devices, collections) }}

<h2>Chip Comparison</h2>
{% set chipIds = getCurrentChips("mac", "desktop") %}
{% set chips = getChips(chipIds, collections) %}
{{ compareTable(chips) }}
