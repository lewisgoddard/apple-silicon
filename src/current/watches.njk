---
layout: base.njk
title: "Current Apple Watch Lineup"
description: "Compare the chips available in every current Apple Watch — Apple Watch Series, Ultra, and SE."
permalink: "current/watches/index.html"
---

{% from "macros/breadcrumbs.njk" import breadcrumbs %}
{% from "macros/compare-table.njk" import compareTable %}
{% from "macros/device-grid.njk" import deviceGrid %}

{{ breadcrumbs([
  { label: "Current Apple Watches" }
]) }}

<h1>Current Apple Watch Lineup</h1>
<p>
  All three current Apple Watch models — Series 11, Ultra 3, and SE 3 — run the S10 SiP. The chip difference between them is zero; what separates the models is the case material, display size, sensors (blood oxygen, depth gauge on Ultra), and battery capacity. If you're choosing based on performance alone, any current model will deliver the same experience.
</p>

{% set devices = getCurrentDevices("watch") %}
{{ deviceGrid(devices, collections) }}

<h2>Chip Comparison</h2>
{% set chipIds = getCurrentChips("watch") %}
{% set chips = getChips(chipIds, collections) %}
{{ compareTable(chips) }}
